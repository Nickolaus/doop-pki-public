# Scripts

Use GitHub Actions or `mise run ...` as the stable interface. Files under this
directory are public-safe implementation details registered in
[`registry.yaml`](./registry.yaml).

Run `mise run scripts:inventory` to inspect ownership and safety metadata.
Run `mise run scripts:check` after moving or editing scripts.
